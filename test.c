#include <stdio.h>

int count_digit(int nb)
{
    int i;

    i = 0;
    while (nb)
    {
        
    }
}

int     check_palindrom(int nb)
{
    int size;

    size = count_digit(nb);
}

int main()
{
    int nb = 16461;
    check_palidrom(nb);
    return (0);
}